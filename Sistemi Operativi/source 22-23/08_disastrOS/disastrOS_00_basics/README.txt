In this folder

- simple structures for memory management

What to see
1. the memory allocation made by a pool allocator
   ( in the kernel we don't have a malloc)
   files: pool_allocator*

2. the double linked list uses some primitive
   form of C-style inheritance for dealing with system structures
   files: linked_list*
   
